@using Asis.Ulasim.Models.Fills
@{
    var _deviceType = new Device();
    IQueryable<DeviceTypeModel> deviceType = null;
    deviceType = _deviceType.FillDeviceType();
}
@*<label class="col-lg-2 col-form-label">
    Device Type
</label>
<div class="col-lg-8 col-md-9 col-sm-12" data-skin="dark" data-toggle="m-tooltip" data-placement="top">
    <select id="deviceType" class="form-control m-bootstrap-select m_selectpicker" multiple data-actions-box="true" data-live-search="true">
        @{
            if (deviceType.Any())
            {
                foreach (var item in deviceType)
                {
                    {
                        <option value='@item.ID'>@item.Name</option>
                    }
                }
            }
            else
            {
                <option value='HI' disabled="disabled"> </option>
            }
        }
    </select>
    <span class="m-form__help">
        Please Select Device Type
    </span>
</div>*@

<div class="form-group">
    <label>Device Type</label>
    <select id="deviceType" class="form-control form-control-solid">
        @{
            if (deviceType.Any())
            {
                foreach (var item in deviceType)
                {
                    {
                        <option value='@item.ID'>@item.Name</option>
                    }
                }
            }
            else
            {
                <option value='HI' disabled="disabled"> </option>
            }
        }
    </select>
</div>

<script type="text/javascript">
    function GetSelectDeviceType() {
        var mSelected = "";
        var lbl = $('#deviceType').val();
        if (lbl === "HI") {
            mSelected = "HI";
        } else {
            var ddl = document.getElementById("deviceType");
            if (ddl.options[0].value === "HI") {
                mSelected = "HI";
            } else if (ddl.selectedIndex === -1) {
                mSelected = "";
            } else {
                var selectedValueArray = [];
                $("#deviceType :selected").each(function() {
                    selectedValueArray.push($(this).val());
                });
                mSelected = selectedValueArray.join(',');
            }
        }
        return mSelected;
    }
</script>




